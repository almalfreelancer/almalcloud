<section class="navbar custom-navbar navbar-fixed-top" role="navigation">
    <div class="container">

        <div class="navbar-header">
            <button class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
                <span class="icon icon-bar"></span>
                <span class="icon icon-bar"></span>
                <span class="icon icon-bar"></span>
            </button>

            <!-- lOGO TEXT HERE -->
            <a href="@Url.Action("Index","Home")" class="navbar-brand">ALMAL for Freelancers</a>
        </div>

        <!-- MENU LINKS -->
        <div class="collapse navbar-collapse">
            <ul class="nav navbar-nav navbar-nav-first">
                <li><a href="@Url.Action("Login","Account")" class="smoothScroll">Home</a></li>
                <li><a href="@Url.Action("Login","Account")" class="smoothScroll">Jobs</a></li>
                <li><a href="@Url.Action("Login","Account")" class="smoothScroll">Task</a></li>
                <li><a href="@Url.Action("Login","Account")" class="smoothScroll">Account</a></li>
            </ul>

            <ul class="nav navbar-nav navbar-right">
                <li><a href="@Url.Action("Login","Account")"><i class="fa fa-lock"></i> Log out</a></li>
            </ul>
        </div>

    </div>
</section>
